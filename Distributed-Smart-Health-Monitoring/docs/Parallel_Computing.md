# Parallel Computing Concept

This project implements parallel computing through the Python Multiprocessing library.

Three independent processes are created:

- Heart Rate Sensor Process
- Temperature Sensor Process
- Oxygen Saturation Sensor Process

Each process executes concurrently and generates monitoring data independently.

The implementation demonstrates how multiple healthcare monitoring tasks can run simultaneously instead of sequentially.